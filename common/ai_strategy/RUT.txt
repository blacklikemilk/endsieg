RUT_we_do_not_guarantee_or_befriend_you = {
	allowed = {
		OR = { #Add anymore tags that guaranteed RUT here
			original_tag = ENG
			original_tag = FRA
			original_tag = SOV
			original_tag = ROM
			original_tag = NOR
			original_tag = SWE
			original_tag = BEL
			original_tag = HOL
			original_tag = SWI
			original_tag = USA
		}
	}

	enable = {
		ROOT = {
			exists = yes
			has_government = democratic
		}
		RUT = {
			exists = yes
			is_subject = no
		}
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = protect
		id = RUT
		value = -250
	}

	ai_strategy = {
    	type = ignore
    	id = RUT
    	value = 500
	}

	ai_strategy = {
		type = befriend
		id = RUT
		value = -500
	}

	ai_strategy = {
		type = alliance
		id = RUT
		value = -500
	}

	ai_strategy = {
		type = diplo_action_desire
		id = RUT
		target = guarantee
		value = -750
	}
}

RUT_i_am_a_push_over = { #Pfot
	allowed = { #Tags most likely to trigger the RUT annexation event
		OR = {
			original_tag = HUN
			original_tag = AUS
			original_tag = ROM
			original_tag = GER
			original_tag = SOV
			original_tag = YUG
			original_tag = ITA
			original_tag = SLO
		}
	}

	enable = {
		RUT = {
			exists = yes
		}
		exists = yes
		has_war_with = RUT
		has_country_flag = CZE_pfot_rut_war_news_event_flag #The is set with more sanity checks in the event
		has_war_with_major = no
		surrender_progress < 0.01
	}

	abort_when_not_enabled = yes

	ai_strategy = {
    	type = front_control
    	state_trigger = {
        	is_controlled_by = RUT
    	}
    	ratio = 0.20
    	ordertype = front
    	execution_type = rush_weak
		execute_order = yes
	}
}